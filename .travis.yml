language: generic

before_install:
  - sudo apt-get -q update
  - sudo apt-get -y install python3.6

matrix:
  include:
    - python: 3.6
      env:
        - TOX_ENV=pep8
    - python: 3.6
      env:
        - TOX_ENV=py36

install:
  - pip install tox

script:
  - tox -e $TOX_ENV
